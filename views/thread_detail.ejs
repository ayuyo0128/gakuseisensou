<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= thread.title %></title>
  <style>
    * { box-sizing: border-box; }
    body {
      font-family: "Yu Gothic UI", sans-serif;
      background-color: #f0f0f0;
      margin: 0;
      padding: 0;
    }

    .back-button {
      display: inline-block;
      margin: 16px;
      color: #0077cc;
      text-decoration: none;
      font-size: 1rem;
    }
    .back-button:hover { text-decoration: underline; }

    .container {
      display: flex;
      max-width: 1200px;
      margin: 0 auto;
    }
    .ad-space {
      width: 15%;
      background-color: #e0e0e0;
      padding: 10px;
      text-align: center;
      font-size: 12px;
    }

    .thread-content {
      flex: 1;
      padding: 20px;
      background-color: white;
      min-height: 100vh;
    }

    .thread-title-area {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-bottom: 30px;
      gap: 1rem;
    }
    .thread-title {
      text-align: center;
      font-size: 2.5rem;
      font-weight: bold;
      color: #333;
      padding: 10px;
    }
    .thread-delete-form {
      display: flex;
      flex-wrap: wrap;
      gap: 0.5rem;
      justify-content: center;
    }
    .thread-delete-form input,
    .thread-delete-form button {
      padding: 8px;
      font-size: 0.9rem;
    }

    .response {
      border-top: 1px solid #ccc;
      padding: 10px 0;
      position: relative;
      font-size: 1rem;
    }
    .response-number {
      color: blue;
      font-weight: bold;
      cursor: pointer;
      position: absolute;
      top: 10px;
      left: 0;
    }
    .response-name {
      margin-left: 30px;
      color:  #2e8b57;
      font-weight: bold;
      font-size: 0.9rem;
    }
    .response-meta {
  margin-left: 40px;
  color: #666; /* グレー（薄い黒） */
  font-size: 0.5rem;
}

    .response-text {
      margin-left: 40px;
      margin-top: 5px;
      line-height: 1.5;
      font-size: 0.1rem;
    }

    .form-area {
      border-top: 1px solid #ccc;
      margin-top: 30px;
      padding-top: 20px;
    }
    .form-area input,
    .form-area textarea,
    .form-area button {
      width: 100%;
      padding: 10px;
      font-size: 1rem;
      margin-bottom: 10px;
    }

    /* ─── スマホ向け調整 ─── */
@media (max-width: 768px) {
  .container {
    flex-direction: column;
    padding: 0;
  }
  .ad-space {
    display: none;
  }
  .thread-content {
    padding: 12px;
  }
  .thread-title {
    font-size: 2rem;
    padding: 8px;
  }
  .thread-delete-form {
    flex-direction: column;
    gap: 0.5rem;
  }
  .thread-delete-form input,
  .thread-delete-form button {
    width: 100%;
    padding: 8px;
    font-size: 0.9rem;
  }
  .response {
    padding: 8px 0;
  }

  /* ↓↓ ここを追加 ↓↓ */
  .response-header {
    margin-left: 0px;
    font-size: 1rem;
    display: flex;
    flex-wrap: wrap;
    gap: 2px;
    align-items: baseline;
  }

  .response-name {
    color: #2e8b57;
    font-weight: bold;
    font-size: 0.8rem;
  }

  .response-id,
  .response-time {
    color: #888;
    font-size: 0.6rem;
  }

  .response-text {
    margin-left: 25px;
    font-size: 0.9rem;
  }

  .form-area {
    margin-top: 20px;
    padding-top: 16px;
  }
  .form-area input,
  .form-area textarea,
  .form-area button {
    font-size: 0.95rem;
    padding: 8px;
  }
}

  </style>
</head>
<body>
  <a href="/clubs/<%= thread.club_id %>" class="back-button">← スレッド一覧に戻る</a>

  <div class="container">
    <div class="ad-space">AD<br>広告スペース</div>
    <div class="thread-content">
      <div class="thread-title-area">
        <div class="thread-title"><%= thread.title %></div>
        <form class="thread-delete-form" action="/threads/<%= thread.id %>/delete" method="POST" onsubmit="return confirm('本当に削除しますか？')">
          <input type="password" name="deletePassword" placeholder="削除用パスワード" required>
          <button type="submit">削除</button>
        </form>
      </div>

      <% responses.forEach(function(response, index) { %>
        <div class="response">
          <div class="response-number" onclick="insertReply(<%= index + 1 %>)">
            <%= index + 1 %>
          </div>
      
          <div class="response-header">
            <span class="response-name"><%= response.name || '名無しの学生' %></span>
            <span class="response-id">[ID:<%= response.anon_id %>]</span>
            <span class="response-time">[<%= response.created_at %>]</span>
          </div>
      
          <div class="response-text">
            <%- (response.text || '').replace(/\n/g, "<br>") %>
          </div>
        </div>
      <% }); %>
      
      <div class="form-area">
        <form action="/alert" method="POST" onsubmit="return validateForm()">
          <input type="hidden" name="threadId" value="<%= thread.id %>">
          <input type="text" name="name" placeholder="名前（任意）">
          <textarea name="content" id="replyBox" rows="4" placeholder="コメントを入力してください" required></textarea>
          <button type="submit">書き込む（確認）</button>
        </form>
      </div>
    </div>
  </div>

  <script>
    function insertReply(number) {
      const replyBox = document.getElementById("replyBox");
      replyBox.value += `>>${number}\n`;
      replyBox.focus();
    }
    function validateForm() {
      if (!document.getElementById("replyBox").value.trim()) {
        alert("コメントを入力してください。");
        return false;
      }
      return true;
    }
  </script>
</body>
</html>
